package Locators

import constructor_classes.LocatorsConstructor

class DialogAndAlertLocators {

    val allowGeoLocationOnUseButton = LocatorsConstructor (
            andriodId = "com.android.permissioncontroller:id/permission_allow_foreground_only_button"
            )
}